<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- favicon -->
    <link rel="shortcut icon" href="./favicon.ico" type="image/x-icon" />
    <!-- FIND A FAVICON -->
    <!-- slogan = find your new best friend -->
    <!-- font awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css"/>
    <!-- normalize css -->
    <link rel="stylesheet" href="./normalize.css" />
    <!-- main css -->
    <link rel="stylesheet" href="./styles.css" />
    <title>Pet Adoption Platform || Pet Profile</title>
</head>
<body class="petProfileBody">
    <a href="/" class="home-link">
        <i class="fas fa-arrow-left"></i>
        Return Home
    </a>
    
    <header class="petProfileHeader">Pet Profile</header>
    
    <div class="pet-info">
        <% if(pet.imageURL) { %>
            <div class="pet-image-container">
                <img src="<%= pet.imageURL %>" alt="Pet Image" class="pet-image">
            </div>
        <% } %>
        <h2 class="pet-name"><%= pet.name %></h2>
        <h3 class="pet-breed"><%= pet.breed %></h3>
        
        <div class="pet-details">
            <div class="detail-item">
                <div class="detail-label">Age</div>
                <div><%= pet.age %></div>
            </div>
            <div class="detail-item">
                <div class="detail-label">Gender</div>
                <div><%= pet.gender %></div>
            </div>
            <div class="detail-item">
                <div class="detail-label">Location</div>
                <div><%= pet.city %>, <%= pet.state %></div>
            </div>
        </div>
    </div>

    <div class="pet-description">
        <div class="detail-label">About</div>
        <p><%= pet.description %></p>
        
        <div class="detail-label">Behavior</div>
        <p><%= pet.behavior %></p>
        
        <div class="detail-label">History</div>
        <p><%= pet.history %></p>
    </div>

    <a href="mailto:<%= pet.email %>" class="contact-button">
        <i class="fas fa-envelope"></i>
        Contact Owner
    </a>
</html>
